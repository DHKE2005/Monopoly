[phases.setup]
nixPkgs = ['php81', 'php81Extensions.pdo', 'php81Extensions.pdo_mysql', 'apache2']

[phases.build]
cmds = ['composer install --no-dev --optimize-autoloader']

[start]
cmd = 'vendor/bin/heroku-php-apache2'